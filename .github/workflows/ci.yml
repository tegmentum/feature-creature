name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  ci:
    name: build + probe
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust (stable, +wasm32)
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 22

      # --- lint ---------------------------------------------------------
      - name: rustfmt
        run: cargo fmt --all --check

      - name: clippy (detector, wasm32)
        run: cargo clippy --lib --release --target wasm32-unknown-unknown -p wasm-feature-detector -- -D warnings

      - name: clippy (component detector, wasm32)
        run: cargo clippy --lib --release --target wasm32-unknown-unknown -p wasm-feature-detector-component -- -D warnings

      - name: clippy (host-wasmtime)
        run: cargo clippy --all-targets --release -p host-wasmtime -- -D warnings

      # --- build --------------------------------------------------------
      - name: Build detector (wasm32-unknown-unknown)
        run: cargo build --release --target wasm32-unknown-unknown -p wasm-feature-detector

      - name: Build component detector (wasm32-unknown-unknown)
        run: cargo build --release --target wasm32-unknown-unknown -p wasm-feature-detector-component

      - name: Report detector size
        run: |
          core=target/wasm32-unknown-unknown/release/wasm_feature_detector.wasm
          comp=target/wasm32-unknown-unknown/release/wasm_feature_detector_component.wasm
          echo "core detector:      $(wc -c <"$core") bytes"      | tee -a "$GITHUB_STEP_SUMMARY"
          echo "component detector: $(wc -c <"$comp") bytes (pre-encode)" | tee -a "$GITHUB_STEP_SUMMARY"

      - name: Build host-wasmtime
        run: cargo build --release -p host-wasmtime

      - name: Emit encoded component (.component.wasm)
        run: |
          cargo run --release -p host-wasmtime -- --emit-component
          out=target/wasm32-unknown-unknown/release/wasm_feature_detector.component.wasm
          echo "encoded component:  $(wc -c <"$out") bytes"       | tee -a "$GITHUB_STEP_SUMMARY"
          # Preamble check: '\0asm' + version=0x000d + layer=0x0001
          head -c 8 "$out" | xxd -p | grep -q '^0061736d0d000100$' || {
            echo "encoded component missing component-model preamble"; exit 1;
          }

      # --- run ----------------------------------------------------------
      - name: host-wasmtime (pretty)
        run: cargo run --release -p host-wasmtime | tee wasmtime-pretty.txt

      - name: host-wasmtime (--json)
        run: |
          cargo run --release -p host-wasmtime -- --json | tee manifest.json
          python3 -c "import json,sys; m=json.load(open('manifest.json')); \
            assert m['schema']=='wasm-feature-detect/capability-manifest/v1', m; \
            assert m['host']['engine']=='wasmtime', m; \
            assert isinstance(m['features'], dict) and m['features'], m; \
            print('manifest ok — %d features, wasmtime %s' % (len(m['features']), m['host']['version']))"

      - name: host-wasmtime rejects unknown args (exit 2)
        run: |
          set +e
          cargo run --release -p host-wasmtime -- --nope 2>err.log
          rc=$?
          set -e
          test "$rc" -eq 2 || { echo "expected exit 2, got $rc"; cat err.log; exit 1; }
          grep -q "usage:" err.log

      # --- codegen up-to-date ------------------------------------------
      - name: js/src/features.js is regenerable
        run: |
          node scripts/gen-features.mjs
          git diff --exit-code -- js/src/features.js

      # --- JS bootstrap smoke test -------------------------------------
      - name: Node smoke test
        run: node js/test/smoke.mjs | tee node-smoke.txt

      - name: host-wasmtime (--component, from encoded artifact)
        run: |
          cargo run --release -p host-wasmtime -- \
            --component \
            target/wasm32-unknown-unknown/release/wasm_feature_detector.component.wasm \
            | tee wasmtime-component.txt
          diff -u wasmtime-pretty.txt wasmtime-component.txt || {
            echo "component surface disagrees with core module"; exit 1;
          }

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: detector-and-outputs
          path: |
            target/wasm32-unknown-unknown/release/wasm_feature_detector.wasm
            target/wasm32-unknown-unknown/release/wasm_feature_detector.component.wasm
            manifest.json
            wasmtime-pretty.txt
            wasmtime-component.txt
            node-smoke.txt
          if-no-files-found: warn
          retention-days: 14

  browser:
    name: browser (chromium)
    runs-on: ubuntu-latest
    needs: ci
    steps:
      - uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 22

      # Pull the detector.wasm built by the `ci` job so this job doesn't
      # need the Rust toolchain. The artifact preserves the target/ layout.
      - name: Download detector artifact
        uses: actions/download-artifact@v5
        with:
          name: detector-and-outputs
          path: .

      - name: Verify detector artifact
        run: |
          ls -l target/wasm32-unknown-unknown/release/wasm_feature_detector.wasm

      - name: Install JS dependencies (playwright)
        working-directory: js
        run: npm ci --no-audit --no-fund

      - name: Install Playwright browsers (chromium + deps)
        working-directory: js
        run: npx playwright install --with-deps chromium

      # The test script spawns js/test/serve.mjs itself on a random port,
      # so there is no separate server-start / server-stop step to babysit.
      - name: Browser smoke test (chromium)
        working-directory: js
        env:
          BROWSERS: chromium
        # GitHub Actions runs `bash -eo pipefail` by default, so a non-zero
        # exit from `node` fails the step even through `tee`.
        run: |
          mkdir -p test/output
          node test/browser.test.mjs 2>&1 | tee test/output/browser-run.txt

      - name: Upload browser artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: browser-outputs
          path: |
            js/test/output/*.json
            js/test/output/*.png
            js/test/output/*.txt
          if-no-files-found: warn
          retention-days: 14
