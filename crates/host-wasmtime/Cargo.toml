[package]
name        = "host-wasmtime"
version     = "0.1.0"
edition.workspace     = true
license.workspace     = true
repository.workspace  = true

[[bin]]
name = "wasm-feature-detect"
path = "src/main.rs"

[dependencies]
wasmtime = { version = "47", default-features = false, features = [
    "std",
    "anyhow",
    "cranelift",
    "runtime",
    "gc",
    "gc-drc",
    "component-model",
    "threads",
    "wat",
] }
anyhow     = "1"
serde      = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
toml       = { version = "0.9", default-features = false, features = ["parse", "serde"] }
# Used only by the `--component` path to encode a raw core wasm (with
# a `component-type` custom section from wit-bindgen) into a valid
# component-model artifact at runtime.
wit-component = "0.254"
